# The app has no app-side Java/Kotlin code of its own — it's a thin Trusted Web
# Activity launcher (see com.google.androidbrowserhelper.trusted.LauncherActivity
# in AndroidManifest.xml). Nothing custom to keep here.
